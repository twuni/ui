import stylish from 'stylish-preact';

export const Viewport = stylish('section', `
  flex: 1;
  display: block;
  margin: 0;
  overflow-x: hidden;
  overflow-y: auto;
  padding: 0;
`);

export default Viewport;
